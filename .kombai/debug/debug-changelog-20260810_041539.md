## Changes Made

### 1. Replaced legacy ESLint compatibility conversion
- **File:** `eslint.config.mjs`
- **Change:** Replaced `FlatCompat` plus `compat.extends("next/core-web-vitals")` with the native flat-config export from `eslint-config-next/core-web-vitals`.
- **Why:** ESLint 10 failed while serializing the legacy compatibility config with a circular React plugin structure, then failed with an incompatible scope manager when the native Next config was used under ESLint 10.
- **Revert:** Restore the `FlatCompat` imports and `compat.extends` call.

### 2. Aligned the ESLint package version with Next 16's flat config
- **Files:** `package.json`, `package-lock.json`
- **Change:** Changed the direct ESLint dev dependency from `^10.8.1` to `^9.7.0` and removed the now-unused `@eslint/eslintrc` dev dependency.
- **Why:** `eslint-config-next@16.3.0` publishes a native flat config and its plugin ecosystem is compatible with ESLint 9, while the installed ESLint 10 release was incompatible with the parser/config stack.
- **Revert:** Restore the previous dependency entries and reinstall.

### 3. Resolved lint errors surfaced after configuration repair
- **File:** `src/app/components/HeroScrollBackground.jsx`
- **Change:** Memoized `renderFrame` with `useCallback` and added its hook dependencies, avoiding ref mutation during render while keeping the progressive scroll-rendering fix stable.
- **Why:** The repaired lint configuration correctly detected a `react-hooks/refs` error and missing effect dependencies.
- **Revert:** Restore the previous render function/ref implementation.

### 4. Escaped an apostrophe in JSX text
- **File:** `src/app/components/Contact.jsx`
- **Change:** Changed `Let's Connect` to `Let&apos;s Connect`.
- **Why:** The repaired lint configuration correctly reported `react/no-unescaped-entities`.
- **Revert:** Restore the literal apostrophe.

## Verification

- `npm.cmd run lint` passes with 0 errors and 2 existing `@next/next/no-img-element` warnings in `Projects.jsx` and `Certifications.jsx`.
- `npm.cmd run build` passes successfully.

## Revert Status

- [ ] Change 1 - Native Next flat config
- [ ] Change 2 - ESLint dependency alignment
- [ ] Change 3 - Hook-safe frame renderer
- [ ] Change 4 - Escaped JSX apostrophe
